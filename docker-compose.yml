services:
  roproxy-lite:
    build: .
    container_name: roproxy-lite
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      TIMEOUT: "5"
      RETRIES: "5"
      # KEY: "your_secret_key"
    restart: unless-stopped
